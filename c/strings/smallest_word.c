#include<stdio.h>
#include<string.h>
#include<ctype.h>
void main()
{
	char s[30],*ptr=s;
	printf("enter a string\n");
	scanf("%[^\n]",s);
	printf("%s\n",s);
	char *smw=s,size=0,in_word=0;
	while(*ptr)
	{
		if(isspace(*ptr))
		{
				
		}
	}
	
	
}	
